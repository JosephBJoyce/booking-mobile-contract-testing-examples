{
  "name": "booking-flights-api",
  "version": "1.0.0",
  "description": "Booking.com Flights REST API — Classic CDC Pact provider",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --testPathPattern=tests/ --forceExit",
    "test:pact": "PACT_PROVIDER_VERIFICATION=true jest --testPathPattern=tests/provider.pact.test.js --forceExit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.1.3",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 60000
  }
}
